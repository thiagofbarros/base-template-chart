apiVersion: v1
kind: Service
metadata:
  name: {{ include "base-template-chart.fullname" . }}
  labels:
    {{- include "base-template-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "base-template-chart.selectorLabels" . | nindent 4 }}
