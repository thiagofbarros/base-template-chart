apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-template-chart.fullname" . }}-test-connection"
  labels:
    {{- include "base-template-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-template-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
